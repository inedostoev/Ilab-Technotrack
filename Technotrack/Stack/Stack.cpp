#include"Stack.h"

/*Верификатор

bool StackOk(const CStack* stack) {
    return (Stack &&
            Stack->count >= 0 &&
            Stack->capacity > Stack->count &&
            Stack->data);
}

define

if(!ok) {
    Dump
    assert(!"Фиаско");
}

__FILE__
__LINE__
__PRETTY_FUNCTION__


Stack s
*(int*)&s = 0
*/
